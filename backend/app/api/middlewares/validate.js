// Middleware commun d’erreurs ajoute au moins

    // errorHandler (catch global),
    //cors (si front Vite sur 5173),
    //éventuellement rateLimit.
    //Ex. CORS minimal :
    //import cors from 'cors'
    //app.use(cors({ origin: 'http://localhost:5173', credentials: true }))